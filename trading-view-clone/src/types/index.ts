export interface CoinData {
    symbol: string
    price: number
    change: number
  }
  
  export interface CandleData {
    time: number
    open: number
    high: number
    low: number
    close: number
  }